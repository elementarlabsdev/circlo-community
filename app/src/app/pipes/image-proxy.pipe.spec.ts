import { ImageProxyPipe } from './image-proxy.pipe';

describe('ImageProxyPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageProxyPipe();
    expect(pipe).toBeTruthy();
  });
});
