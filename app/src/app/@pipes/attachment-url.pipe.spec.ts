import { MediaItemUrlPipe } from './attachment-url.pipe';

describe('MediaItemUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new MediaItemUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
