import { ConvertCodePipe } from './convert-code.pipe';

describe('ConvertCodePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertCodePipe();
    expect(pipe).toBeTruthy();
  });
});
